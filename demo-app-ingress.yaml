apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: testapp
  namespace: demo-app
spec:
  ingressClassName: nginx
  rules:
  - host: testapp.10.100.52.147.nip.io
    http:
      paths:
      - backend:
          service:
            name: frontend
            port:
              number: 80
        path: /
        pathType: Prefix
